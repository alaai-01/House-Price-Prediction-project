# Pinned to the versions the model was trained/served with.
# scikit-learn MUST match the notebook's version or the pickle may fail to load.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.10.3
pydantic-settings==2.6.1
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
joblib==1.4.2

# --- test dependencies ---
pytest==8.3.4
httpx==0.28.1
